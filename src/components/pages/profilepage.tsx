import { Header } from '../header/header';
import { Profile } from '../profile/profile';

export const ProfilePage = () => {
    return (
        <>
            <Header />
            <Profile />
        </>
    );
};
