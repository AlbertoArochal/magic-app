import { MainPage } from '../pages/mainpage';
import { Header } from '../header/header';

export const App = () => {
    return (
        <div>
            <Header />
            <MainPage />
        </div>
    );
};
